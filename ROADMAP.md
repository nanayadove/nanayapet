# NetPet 演进路线图

## ✅ 阶段一：框架迁移 [已完成]
从 PyQt5 迁移到 Electron + JavaScript，重构核心架构。

- [x] Electron 桌面窗口（无边框透明、置顶、拖拽）
- [x] 聊天气泡 + 输入框 + 情绪驱动立绘切换
- [x] SQLite 记忆系统（sql.js，对话存库 + 自动上下文加载）
- [x] 独立设置窗口（Provider/Key/模型/温度/总结间隔）
- [x] 后台自动总结记忆（每 N 轮触发一次）
- [x] Config 驱动 UI 尺寸

## 🔄 阶段二：Agent 工具系统 [规划中]

核心改造：从纯粹聊天机器人升级为可调用工具的 Agent。

### 架构目标
```
用户输入
  ├→ 工具提取模型（便宜模型）→ 分析是否需要调用工具
  │                               ↓
  │                           执行工具（write_file / schedule 等）
  │                               ↓
  └→ 聊天模型（主模型） → 结合工具结果生成回复
```

### 工具列表（初期）
- [ ] `write_file` — 记录待办、笔记、日记等任意信息
- [ ] `read_file` — 回顾已记录的内容
- [ ] `schedule` — 注册定时提醒（到点触发 LLM 主动发言）
- [ ] `complete_task` — 标记提醒/待办为已完成

### 技术要点
- [ ] 两步推理：工具模型 + 聊天模型分离，避免工具失败但回复说"已搞定"
- [ ] 工具执行结果注入下一轮上下文
- [ ] System Prompt 中声明工具列表，LLM 自主决定何时调用
- [ ] 所有工具通用化设计（不写死具体功能，让 LLM 自己组合使用）

## 🔜 阶段三：增强交互 [待定]

- [ ] 定时触发主动提醒（基于 schedule 工具 + cron）
- [ ] 闲聊式主动搭话（低活跃度定时器）
- [ ] 历史对话语义搜索（RAG 检索旧记忆）
- [ ] 立绘动画/GIF 支持
